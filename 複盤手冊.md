# Project F.O.X. — 策略複盤手冊

目標一句話：**找出「哪種開倉條件的期望值是負的」**，然後把那些條件收緊或排除。

> 重點不是看勝率，是看**平均盈虧（期望值）**。哪一組的平均盈虧是負的，那組條件就是漏財點。

> ⚠ **手續費盲點（必讀）**：`trade_history.pnl` 是**毛利（只有價差）**，引擎平倉時另外扣的開倉/平倉手續費（各 0.05%，來回約 0.10% 名目）與資金費**沒寫進資料表**。所以本手冊下面所有 SQL 直接 `SUM(pnl)/AVG(pnl)` 都是**毛利、會高估**。
>
> 想看「扣費後真實期望值」請改跑 `python analyze_entries.py`——它由 `qty = pnl/價差` 反推名目、估回手續費，列出每組的**淨期望值**並用 `⚠負` 標出扣費後為負的組別。實測 40 筆時毛利 +429 但手續費吃掉 -205，淨利只剩 +224（費用佔毛利約一半），這條策略的毛 edge 其實很薄。
>
> （長期正解：引擎在 `insert_trade` 時改存「淨損益」欄位，dashboard / AI 副駕的 SQL 才會自動是扣費後的真值。）

---

## 使用前提

- 資料表：`trade_history`，欄位含 `side / pnl / score / exit_reason / rsi / cci / vol_surge / trend_gap / leverage`。
- `rsi/cci/vol_surge/trend_gap/leverage` 為 2026-06 後新增，舊紀錄為 0，分析時用 `欄位 != 0` 過濾。
- `timestamp` 只有 `HH:MM:SS`（無日期），不要做跨日分析。
- 透過 AI 副駕（「🤖 AI 副駕」分頁）問下列白話問題即可；副駕會自動加上 `WHERE user_id = 你的id`。

## 心法（務必遵守）

1. **先看樣本數**：任何一組 < 20~30 筆都別當真，那是雜訊。
2. **找負期望值**：盯「平均盈虧」欄，負的那組就是要處理的條件。
3. **一次只改一個變數**，改完再跑一輪驗證，別一次調五個參數。

---

## A. 體質總覽

**問**：各平倉原因的次數、勝率與總盈虧統計

```sql
SELECT exit_reason, COUNT(*) AS 筆數,
       ROUND(100.0*SUM(CASE WHEN pnl>0 THEN 1 ELSE 0 END)/COUNT(*),1) AS 勝率,
       ROUND(SUM(pnl),2) AS 總盈虧, ROUND(AVG(pnl),2) AS 平均盈虧
FROM trade_history GROUP BY exit_reason ORDER BY 總盈虧;
```

## B. 多空對稱性（驗證「做多偏向」是否為漏財點）

**問**：做多與做空分開，各自的勝率和期望值（平均盈虧）

```sql
SELECT side, COUNT(*) AS 筆數,
       ROUND(100.0*SUM(CASE WHEN pnl>0 THEN 1 ELSE 0 END)/COUNT(*),1) AS 勝率,
       ROUND(AVG(pnl),2) AS 期望值, ROUND(SUM(pnl),2) AS 總盈虧
FROM trade_history GROUP BY side;
```

## C. 趨勢濾網有效性 ★最關鍵

**問**：依趨勢偏離分強下跌(<-3%)、盤整、強上升(>3%)三組，做多的平均盈虧與勝率

```sql
SELECT CASE WHEN trend_gap < -3 THEN '1.強下跌'
            WHEN trend_gap >  3 THEN '3.強上升'
            ELSE '2.盤整' END AS 趨勢分組,
       COUNT(*) AS 筆數, ROUND(AVG(pnl),2) AS 平均盈虧,
       ROUND(100.0*SUM(CASE WHEN pnl>0 THEN 1 ELSE 0 END)/COUNT(*),1) AS 勝率
FROM trade_history WHERE side='Long' AND trend_gap!=0
GROUP BY 趨勢分組 ORDER BY 趨勢分組;
```

→ 若「強下跌」組最慘，代表濾網方向對，甚至可把門檻從 ±3% 收更緊。

## D. 共振評分有無預測力

**問**：共振評分分成 <60、60-79、80+ 三層，各層的勝率和平均盈虧

```sql
SELECT CASE WHEN score>=80 THEN '3.絕殺(80+)'
            WHEN score>=60 THEN '2.標準(60-79)'
            ELSE '1.試水(<60)' END AS 評分層,
       COUNT(*) AS 筆數, ROUND(AVG(pnl),2) AS 平均盈虧,
       ROUND(100.0*SUM(CASE WHEN pnl>0 THEN 1 ELSE 0 END)/COUNT(*),1) AS 勝率
FROM trade_history GROUP BY 評分層 ORDER BY 評分層;
```

→ 若高分組沒有比較好，代表評分公式失效、要重做。

## E. RSI 接刀深度

**問**：做多交易按 RSI 分 <15、15-20、20-25、25-30 四桶，各桶平均盈虧

```sql
SELECT CASE WHEN rsi<15 THEN '1.<15' WHEN rsi<20 THEN '2.15-20'
            WHEN rsi<25 THEN '3.20-25' ELSE '4.25-30' END AS RSI桶,
       COUNT(*) AS 筆數, ROUND(AVG(pnl),2) AS 平均盈虧
FROM trade_history WHERE side='Long' AND rsi!=0
GROUP BY RSI桶 ORDER BY RSI桶;
```

→ 決定「RSI<30」要不要收緊成「RSI<20 才進場」。

## F. 槓桿 vs 爆倉

**問**：不同槓桿倍數的爆倉次數佔比和平均盈虧

```sql
SELECT leverage, COUNT(*) AS 筆數,
       SUM(CASE WHEN exit_reason='爆倉' THEN 1 ELSE 0 END) AS 爆倉數,
       ROUND(AVG(pnl),2) AS 平均盈虧
FROM trade_history WHERE leverage!=0 GROUP BY leverage ORDER BY leverage;
```

## G. 黑名單幣種

**問**：每個幣種的總盈虧與交易次數，由賠最多排到賺最多（至少3筆）

```sql
SELECT symbol, COUNT(*) AS 筆數, ROUND(SUM(pnl),2) AS 總盈虧
FROM trade_history GROUP BY symbol HAVING COUNT(*)>=3 ORDER BY 總盈虧;
```

---

## 複盤後的調整方向（對照表）

| 發現 | 可能的調整 |
|------|-----------|
| 做多期望值為負（B） | 加強趨勢濾網 / 收緊 RSI 門檻 / 暫停做多 |
| 強下跌組最慘（C） | 趨勢濾網門檻收緊（±3% → ±2%） |
| 高評分組沒比較好（D） | 重新設計共振評分公式 |
| RSI 越深越賺（E） | 進場門檻改 RSI < 20 |
| 高槓桿爆倉率高（F） | 降低預設槓桿 / 依評分動態調槓桿 |
| 特定幣一直賠（G） | 加入掃描黑名單 |

> 提醒：以上每一項都要先有足夠樣本（建議單組 ≥ 30 筆）才有統計意義。樣本不足時的結論一律視為雜訊。
